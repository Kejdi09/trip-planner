# Local backend port
PORT=3001

# MongoDB Atlas connection string (replace placeholders)
# Example:
# mongodb+srv://<username>:<password>@<cluster-url>/tripplanner?retryWrites=true&w=majority&appName=TripPlanner
MONGODB_URI=

# Optional: explicit DB name if not provided in URI
MONGODB_DB_NAME=tripplanner
